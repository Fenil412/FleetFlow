# ============================================================
# FleetFlow — FRONTEND (Vite) Environment Variables
# Copy this file to frontend/.env and fill in your values.
# All frontend env vars MUST start with VITE_ to be exposed.
# NEVER commit the real .env file to version control.
# ============================================================

# ── API & Socket Connection ───────────────────────────────────
# URL of the FleetFlow backend server
VITE_API_BASE_URL=http://localhost:5001/api
VITE_SOCKET_URL=http://localhost:5001

# ── App Info (optional, used in meta/titles) ──────────────────
VITE_APP_NAME=FleetFlow
VITE_APP_VERSION=1.0.0
