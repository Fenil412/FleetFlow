# ============================================================
# FleetFlow — SERVER Environment Variables
# Copy this file to .env and fill in your actual values.
# NEVER commit the real .env file to version control.
# ============================================================

# ── Server ───────────────────────────────────────────────────
PORT=5001
NODE_ENV=development

# ── AI Service Connection ──────────────────────────────────────
# URL of the FleetFlow AI FastAPI service
# Production Example:
# AI_SERVICE_URL=https://ai.yourdomain.com
AI_SERVICE_URL=http://localhost:8001

# ── PostgreSQL Database (Neon) ────────────────────────────────
# Use your Neon connection string here.
DATABASE_URL=postgresql://neondb_owner:npg_...-pooler.aws.neon.tech/neondb?sslmode=require


# ── JWT Authentication ────────────────────────────────────────
# Use a long, random string (min 32 characters)
JWT_SECRET=change_me_to_a_long_random_secret_key_at_least_32_chars
JWT_EXPIRY=7d

# ── Cloudinary (User Avatar / Photo Upload) ───────────────────
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ── EmailJS (Email Notifications) ─────────────────────────────
# Get these from: https://dashboard.emailjs.com/
EMAILJS_SERVICE_ID=your_service_id_here
EMAILJS_PUBLIC_KEY=your_public_key_here
EMAILJS_PRIVATE_KEY=your_private_key_here
EMAILJS_TEMPLATE_WELCOME=welcome_email
EMAILJS_TEMPLATE_OTP=otp_email
EMAILJS_NOTIFICATION_EMAIL=your_notification_email@gmail.com



# ── CORS ──────────────────────────────────────────────────────
# Set to your frontend origin in production (comma-separated for multiple)
# Production Example:
# CORS_ORIGIN=https://www.yourdomain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# ── Security ──────────────────────────────────────────────────
BCRYPT_SALT_ROUNDS=10
